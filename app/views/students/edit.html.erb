<%= link_to "View all Students",  students_path(@student) %>
<%= link_to "Create new student",  new_student_path(@student) %>



<%= form_for @student do |f| %>
  <%= f.label :name %>
  <%= f.text_field :name %>

  <%= f.submit %>
<% end %>
